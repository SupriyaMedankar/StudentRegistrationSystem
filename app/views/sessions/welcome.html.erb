<div>
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  <h1 class="font-bold text-4xl">Welcome</h1>

  <%= button_to "Login", '/login', method: :get, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
  <%= button_to "Sign Up", '/users/new', method: :get, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
</div>
